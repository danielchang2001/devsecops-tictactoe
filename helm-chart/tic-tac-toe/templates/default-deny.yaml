apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: default
spec:
  podSelector: {{ .Values.defaultDeny.podSelector | nindent 2 }}
  policyTypes:
    - Ingress
