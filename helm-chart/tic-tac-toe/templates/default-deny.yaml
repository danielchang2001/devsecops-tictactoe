apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: default
spec:
  {{- with .Values.defaultDeny.podSelector }}
  podSelector: {{ toYaml . | nindent 2 }}
  {{- end }}
  policyTypes:
    - Ingress
